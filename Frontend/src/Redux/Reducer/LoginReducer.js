

const defaultState = {
    GetLoginApi : [],
    res:""
}

export const LoginReducer = (state = defaultState,action)=> {
     switch (action.type) {
       
     
        default:
            return state
     }
}